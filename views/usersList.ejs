<%- include('include/top.ejs') %> <% if (users.length > 0) { %>
<ul>
  <% for (user of users) { %>
  <li><%= user.user %></li>
  <% } %>
</ul>
<% } else { %>
<p>No user found!</p>
<% } %> <%- include('include/bottom.ejs') %>
